name: macOS

"on":
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 --version
      - run: chmod +x pyinit test_pyinit.sh
      - run: ./test_pyinit.sh
